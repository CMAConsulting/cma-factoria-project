{
  "name": "@cma-factoria/shared-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "openapi-generator-cli generate -c openapi.config.yaml",
    "build": "npm run generate && tsc",
    "clean": "rm -rf dist && rm -f *.js *.d.ts apis/*.js apis/*.d.ts models/*.js models/*.d.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.2",
    "@openapitools/openapi-generator-cli": "^2.15.0"
  }
}
